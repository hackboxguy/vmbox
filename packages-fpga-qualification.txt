# Dedicated Gate-3a image: it deliberately excludes unrelated RH850 and
# terminal applications, so FPGA hardware qualification is reproducible with
# only the pinned FPGA sources present.
# NAME|GIT_REPO|VERSION|CMAKE_OPTIONS|BUILD_DEPS|PORT|PRIORITY|TYPE|DESCRIPTION
fpga-flasher-webapp|file://../fpga-flasher-webapp|a5427ca06a354022d7edabf47069b8b2433ddcfe||cmake,python3|8006|29|webapp|FPGA Configuration Flasher
